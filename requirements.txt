# 柒月·合一 依赖

# ─── 核心 ───
openai>=1.0.0           # LLM SDK (DeepSeek 兼容)

# ─── 网关层 ───
aiohttp>=3.9            # HTTP 服务器 + 客户端
pycryptodome>=3.20      # 钉钉消息加解密

# ─── 思维层 ───
pydantic>=2.0           # 数据校验

# ─── 执行层 ───
playwright>=1.48        # 浏览器自动化
httpx>=0.27             # Web 请求
pyyaml>=6.0             # SKILL.md 解析

# ─── 记忆层 ───
# SQLite3 内置，无需额外安装

# ─── 开发 ───
pytest>=8.0
pytest-asyncio>=0.24
